apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

# Override the image tag here for each environment, e.g.:
# images:
#   - name: scoville
#     newName: ghcr.io/jalapeno/scoville
#     newTag: "0.1.0"
